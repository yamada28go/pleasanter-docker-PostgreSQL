# Project-specific PostgreSQL settings.
#
# Runtime-tunable values such as WAL/archive settings are injected from
# `docker-compose.yml` via `postgres -c ...` so they can be controlled
# through `.env`.
